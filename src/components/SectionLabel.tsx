export default function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <span className="text-[12px] font-medium uppercase tracking-[0.15em] text-[#F28C38] mb-4 block">
      // {children}
    </span>
  );
}
